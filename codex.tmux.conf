# Minimal tmux config for codex sessions.
# This file intentionally avoids any custom keybinds.
set -g status off
set -g mouse on
set -g allow-rename off
set -g base-index 1
setw -g aggressive-resize on
set -s escape-time 50
